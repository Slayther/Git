git config --global alias.aliasCommandName '!non-git-command && git command'

# Push
git add *
git commit -m "Commit message"
git push origin master

# Remove ignored files
git rm -r --cached .
git add *
git commit -am "Remove ignored files"
git push origin master
